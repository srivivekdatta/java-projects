package com.learning.packt;

import java.util.ArrayList;
import java.util.Comparator;
import java.util.List;

public class MainClass {

	/*
	 * public static void main(String[] args) { //String[] args1 = { "Vivek",
	 * "Datta", "Sri" }; // SortByLength.sortArrayByLength(args1, "ASC");
	 * //System.out.println(SubString.contains("hello world", "llo"));
	 * //CountOfSubString.countStringInString("VivekDatta", "Vivek" );
	 * //Anagrams.isAnagram("School MASTER", "The ClassROOM"); //
	 * /GenareateNsttrings.nStrings("Datta"); //StripTrim.trim("help me     ");
	 * 
	 * // create an ArrayList List<String> languages = new ArrayList<>();
	 * 
	 * // add elements to ArrayList languages.add("Python"); languages.add("Swift");
	 * languages.add("C"); languages.add("JavaScript");
	 * System.out.println("Unsorted ArrayList: " + languages);
	 * 
	 * // sort the ArrayList in ascending order
	 * languages.sort(Comparator.reverseOrder());
	 * 
	 * languages.removeIf(num -> num.startsWith("J"));
	 * System.out.println("Sorted ArrayList: " + languages); }
	 */
	
	/*
	 * // create an object of list using ArrayList static List<String> places = new
	 * ArrayList<>();
	 * 
	 * // preparing our data public static List<String> getPlaces(){
	 * 
	 * // add places and country to the list places.add("Nepal, Kathmandu");
	 * places.add("Nepal, Pokhara"); places.add("India, Delhi");
	 * places.add("USA, New York"); places.add("Africa, Nigeria");
	 * 
	 * return places; }
	 * 
	 * public static void main( String[] args ) {
	 * 
	 * List<String> myPlaces = getPlaces();
	 * System.out.println("Places from Nepal:");
	 * 
	 * // Filter places from Nepal myPlaces.stream() .filter((p) ->
	 * p.startsWith("Nepal")) .map((p) -> p.toUpperCase()) .sorted() .forEach((p) ->
	 * System.out.println(p)); }
	 */
	
	// invoke HashMap static method
	
	/*
	 * public static void main(String[] args) { HashMaptest.hasmaptest(); }
	 */
	
	// invoke Arrays main class
	


	}

